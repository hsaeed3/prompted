"""
prompted.types.openai

Contains the lowest level definitions for completion types defined within
the `prompted` framework. As `prompted` uses `litellm` as the backend
depedency, the framework standardizes on the OpenAI Chat Completions
API.
"""
